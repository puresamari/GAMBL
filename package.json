{
  "scripts": {
    "app:start": "npm --prefix=app/ start",
    "app:test": "npm run --prefix=app/ test",
    "start": "anchor localnet && npm run app:start",
    "test": "anchor test",
    "build": "anchor build",
    "deploy": "anchor deploy"
  },
  "dependencies": {
    "@project-serum/anchor": "^0.19.0"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^8.0.0",
    "@types/mocha": "^9.0.0",
    "typescript": "^4.3.5"
  }
}